{
  "buildCommand": "cd platform/frontend/web && npm install && npm run build",
  "outputDirectory": "platform/frontend/web/dist",
  "installCommand": "echo skipping",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
